
{{#extends "base"}}

{{#block "content"}}
<div class="container-fluid">
    <div class="row">
        <div class="col-3 px-1 bg-dark" id="sticky-sidebar">
            <div class="nav flex-column flex-nowrap vh-100 overflow-auto text-white p-2">
                <h2>Search</h2>
                <form method="GET">
                    {{{searchForm}}}
                    <input type="submit" class="btn btn-primary mt-3"/> 
                </form>
            </div>
        </div>
        <div class="col-9" id="main">
            {{!-- <div class="table-container fluid" > <!-- Separate div for table area -->
                <table class="table table-secondary table-striped-columns">
                    <thead>
                        <th>Product ID</th>
                        <th>Name</th>
                        <th>Cost</th>
                        <th>Description</th>
                        <th>Category</th>
                        <th>Tags</th>
                        <th></th>
                    </thead>
                    {{#each products}}
                    <tr>
                        <td>{{this.id}}</td>
                        <td>{{this.name}}</td>
                        <td>{{this.cost}}</td>
                        <td>{{this.description}}</td>
                        <td>{{this.category.name}}</td>
                        <td>
                            {{#each this.tags}}
                            <span class="badge bg-primary">{{this.name}}</span>
                            {{/each}}
                        </td>
                        <td>
                            <a href="/products/update-product/{{this.id}}" class="btn btn-primary btn-sm">Update</a>
                            <a href="/products/delete-product/{{this.id}}" class="btn btn-warning btn-sm">Delete</a>
                            <a href="/cart/{{this.id}}/add" class="btn btn-success btn-sm">Add To Cart</a>
                        </td>
                    </tr>
                    {{/each}}
                </table>
            </div> --}}
    <section style="background-color: #eee;">
  <div class="text-center container py-5">
    <h4 class="mt-4 mb-5"><strong>Pets</strong></h4>
 {{#each products}}
    <div class="row">
       
      <div class="col-lg-4 col-md-12 mb-4">
        
        <div class="card">
          <div class="bg-image hover-zoom ripple ripple-surface ripple-surface-light"
            data-mdb-ripple-color="light">
            <img src="{{this.image_url}}"
              class="w-100" />
            <a href="#!">
              <div class="mask">
                <div class="d-flex justify-content-start align-items-end h-100">
                  <h5><span class="badge bg-primary ms-2">New</span></h5>
                </div>
              </div>
              <div class="hover-overlay">
                <div class="mask" style="background-color: rgba(251, 251, 251, 0.15);"></div>
              </div>
            </a>
          </div>
          <div class="card-body">
            <a href="" class="text-reset">
              <h5 class="card-title mb-3">{{this.name}}</h5>
            </a>
            <a href="" class="text-reset">
              <p>{{this.category.name}}</p>
            </a>
            <h6 class="mb-3">{{this.cost}}</h6>
            {{!-- <div>
            <a href="/products/update-product/{{this.id}}" class="btn btn-primary btn-sm">Update</a>
          </div>
          <a href="/products/delete-product/{{this.id}}" class="btn btn-warning btn-sm">Delete</a> --}}
             <a href="/cart/{{this.id}}/add" class="btn btn-success btn-sm">Add To Cart</a>
          </div>
          
        </div>
      </div>
    </div>
     {{/each}}
  </div>
</section>
        </div>
    </div>
</div>
{{/block}}

{{/extends}}